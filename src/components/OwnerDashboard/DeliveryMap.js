
"use client";

// This component is temporarily disabled due to build issues.
// We will explore a more stable map solution in the future.

export default function DeliveryMap({ boys, restaurantLocation, trackedBoyId }) {
    return (
        <div className="w-full h-full bg-gray-700 flex items-center justify-center text-gray-400">
            <p>Map feature is temporarily unavailable.</p>
        </div>
    );
}
    