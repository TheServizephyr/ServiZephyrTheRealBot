Desktop build resources live here.

Current app window icon uses `public/logo.png` from the Electron main process.

Windows packaging now points to `desktop/assets/icon.png`, copied from `public/logo.png`.

If needed later, this can still be replaced with a handcrafted `icon.ico`.
